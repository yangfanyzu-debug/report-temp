# 双类型 AI 审核发布检查表

## 数据库

- [ ] 查询运行中审核数量为 0。
- [ ] 停止 `report-management-worker.service`。
- [ ] 备份远端数据库和当前后端 release。
- [ ] 执行 `006_precheck_dual_audit_types.sql`。
- [ ] 人工确认 `unlinked_audit_rows = 0`，并记录当前启用提示词数量。
- [ ] 获得数据库迁移明确批准。
- [ ] 执行 `006_dual_audit_types.sql`。
- [ ] 验证 `initial` 和 `revision` 启用提示词各 1 条、历史审核类型无空值、共用模型配置存在。

## 服务

- [ ] 部署后端并重启 API 与 Worker。
- [ ] 模拟批次登记，确认 `auditType=initial`。
- [ ] 上传用户版本，确认 `auditType=revision`。
- [ ] 确认两类任务读取同一模型连接但使用各自提示词。
- [ ] 确认历史审核和检查点快照仍可查看。

## 前端

- [ ] 部署本地已验证的 `ruoyi-ui/dist` 静态资源。
- [ ] 验证共享模型连接配置可保存且不显示明文 Key。
- [ ] 验证初始审核和修订审核提示词可分别保存新版本。
- [ ] 验证报告详情与审核工作台显示审核类型。
- [ ] 保留静态资源回滚目录和数据库备份路径。

生产数据库迁移、服务重启和静态资源替换必须在用户再次明确确认后执行。
