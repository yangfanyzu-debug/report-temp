# Version Report Files Instead Of Replacing Them

Uploaded report files are always stored as new Report Versions and never replace an existing file, even when the uploaded DOCX has the same original filename. This differs from the old standalone DOCX file service because report review needs stable access to the initial batch output, every uploaded revision, and the AI audit result tied to each version.
